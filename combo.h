#include <iostream>
#include <string>
#include <vector>

#ifndef COMBO_H_
#define COMBO_H_

//int combine(int, int);
std::string combine(int, int);
std::string combine(const std::string&, const std::string&);
std::string combine(const std::string&, int);

#endif